<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs') %>
<body>
<%- include('../partials/nav.ejs') %>

    <div class="blogs content">
        <h2>All Blogs</h2>
        <% if (blogs.length > 0) { %> <!-- Corrected spelling of 'length' -->
            <% blogs.forEach(blog => { %> <!-- Fixed missing closing %> -->
                <h3 class="title"><%= blog.title %></h3> <!-- Correctly place `<%= blog.title %>` -->
                <p class="snippet"><%= blog.snippet %></p> <!-- Correctly place `<%= blog.snippet %>` -->
            <% }) %>
        <% } else { %> <!-- Fixed 'else' typo -->
            <p>There are no blogs to display...</p>
        <% } %>
        
    </div>
    <%- include('../partials/footer.ejs') %>
</body>
</html>